[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short
markers =
    unit: isolated domain and application tests
    api: FastAPI contract tests with dependency overrides
    e2e: in-memory cross-module scenarios
    integration: live-postgres constraint tests