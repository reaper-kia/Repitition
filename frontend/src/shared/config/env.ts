export const config = {
  /** Интервал поллинга в админке, мс. Пригодится как fallback, если WebSocket ляжет. */
  pollingMs: 25_000,
  /** Размер страницы в списках по умолчанию. */
  pageSize: 24,
};
